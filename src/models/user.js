export default class User {
    constructor(username, email, roles, token, password) {
      this.username = username;
      this.email = email;
      this.roles = roles;
      this.password = password;
    }
  }
